Check if the AI context files (CLAUDE.md, README.md, .cursorrules, .github/copilot-instructions.md) are out of date compared to the current codebase.

1. Read the current project structure under `src/` and compare it against what is documented in CLAUDE.md.
2. If any files, pages, layouts, dependencies, or patterns have changed and are not reflected in the context files, report what is out of date.
3. If everything is in sync, say so.

Do NOT modify any files — only report the diff between reality and documentation.
